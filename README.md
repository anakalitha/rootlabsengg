# RootLabs Engineering — Next.js Modern Theme

This is a Next.js (App Router) rewrite of the original PHP/HTML RootLabs Engineering site,
using the same Nunito font and the modern blue theme as the RootLabs Grain Mill site.

## Run locally

npm install
npm run dev
